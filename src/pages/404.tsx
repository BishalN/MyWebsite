import { GlobalLayout } from '../components/GlobalLayout';

const NotFoundPage = () => {
  return (
    <GlobalLayout>
      <div>The page we're looking for was not found</div>
    </GlobalLayout>
  );
};

export default NotFoundPage;
